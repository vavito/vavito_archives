{
  "name": "@vavito/e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "test": "node --test src/*.test.mjs",
    "test:public": "playwright test --config playwright.config.mjs",
    "test:auth": "playwright test --config auth.playwright.config.mjs",
    "test:admin": "playwright test --config admin.playwright.config.mjs",
    "test:a11y": "playwright test accessibility.spec.mjs --config playwright.config.mjs && playwright test accessibility.spec.mjs --config admin.playwright.config.mjs",
    "test:full-stack": "pnpm run test:public && pnpm run test:auth && pnpm run test:admin"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "^1.62.1",
    "@vavito/eslint-config": "workspace:*",
    "eslint": "9.39.5"
  }
}
